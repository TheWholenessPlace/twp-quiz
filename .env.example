# Formspree endpoint that delivers quiz + intake records to
# info@thewholenessplace.com. Defaults to https://formspree.io/f/meeyezrz
# (set in src/config.js) if left blank here — only set this to override
# with a different form.
VITE_FORMSPREE_ENDPOINT=
